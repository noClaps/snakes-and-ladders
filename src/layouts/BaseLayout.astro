---
import Footer from "../components/layout/Footer.astro";
import Head from "../components/layout/Head.astro";
import "../styles/styles.css";

interface Props {
    title?: string;
    description?: string;
}

const {
    title = "Snakes and Ladders",
    description = "A simple game of snakes and ladders made with Astro",
} = Astro.props;
---

<!doctype html>
<html lang="en">
    <Head {title} {description}>
        <slot name="head" />
    </Head>
    <body>
        <slot />
        <style>
            body {
                background-color: var(--background);
                color: var(--text);
                min-height: 100dvh;
                display: grid;
                grid-template-rows: 1fr auto;
                margin: 0;
            }
        </style>
        <Footer />
    </body>
</html>
