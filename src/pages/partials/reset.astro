---
import Board from "../../components/game/Board.astro";
import {
    p1position,
    p2position,
    p3position,
    p4position,
    turn,
} from "../../scripts/position";

export const partial = true;

p1position.set(0);
p2position.set(0);
p3position.set(0);
p4position.set(0);
turn.set(1);
---

<Board playerPositions={[0, 0, 0, 0]} />
