<button
    id="reset"
    hx-get="/partials/reset"
    hx-target="#board"
    hx-swap="outerHTML"
    hx-trigger="click, load once">Reset game</button
>

<style>
    button {
        font-size: 1.125rem;
        padding: 0.5rem;
        cursor: pointer;
        border: none;
        background-color: var(--danger);
        color: var(--accent-text);
        border-radius: 0.5rem;
        transition: var(--transition);

        &:hover {
            box-shadow: 0 0 1rem 0.25rem var(--danger);
        }
    }
</style>
